from setuptools import setup, find_packages

setup(
    name="llm_mafia_backend",
    version="0.1.0",
    packages=find_packages(),
    # Add other dependencies if needed, or rely on requirements.txt
    # install_requires=[
    #     # List dependencies here, e.g.,
    #     # 'fastapi',
    #     # 'pydantic',
    # ],
) 